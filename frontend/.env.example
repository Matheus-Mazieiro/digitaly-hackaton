# Sinalização WebRTC.
# Deixe VAZIO para testar em duas abas do mesmo navegador (BroadcastChannel).
# Aponte para o WebSocket do backend Go em produção.
# Exemplo: VITE_SIGNALING_URL=wss://api.digitaly.tech/ws/signal
VITE_SIGNALING_URL=